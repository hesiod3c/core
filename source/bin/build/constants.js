import path from 'path';

export const repoRoot = path.resolve(__dirname, '../../');

export const srcRoot = path.join(repoRoot, 'shared/');
export const imagesRoot = path.join(repoRoot, 'shared/images');
export const libRoot = path.join(repoRoot, 'lib/');
export const imagesDist = path.join(repoRoot, 'lib/images');
